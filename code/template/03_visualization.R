###############################################################################
# Data visualization for `[TODO]`
# © ALARM Project, November 2021
###############################################################################

# TODO Define the koiki-renkei areas (広域連携)
a_koiki <- c()
b_koiki <- c()
c_koiki <- c()
koiki <- c(a_koiki, b_koiki, c_koiki)

for (i in 0:n_split)
{
  pref_smc_plans_n <- redist::get_plans_matrix(sim_smc_pref_0)
}

